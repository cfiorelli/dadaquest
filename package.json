{
  "name": "dada-quest",
  "version": "1.0.0",
  "description": "Da Da Quest - A comedic 2D platformer",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pw:install": "playwright install --with-deps chromium",
    "test:smoke": "playwright test -g smoke --project=chromium",
    "test": "npm run build && npm run test:smoke",
    "screenshot": "npm run build && npm run preview -- --host 127.0.0.1 --port 4173 & sleep 3 && playwright test --config playwright.screenshot.config.js; kill %1",
    "shots": "npm run build && playwright test --config playwright.screenshot.config.js"
  },
  "dependencies": {
    "@babylonjs/core": "^6.28.0",
    "@babylonjs/gui": "^6.28.0",
    "@babylonjs/loaders": "^6.28.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "vite": "^5.4.2"
  }
}
